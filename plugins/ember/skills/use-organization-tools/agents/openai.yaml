interface:
  display_name: "Use organization skills and tools"
  short_description: "Find workflows and use approved tools"
  default_prompt: "Use $use-organization-tools to find an available organization skill or approved tool for my task."
dependencies:
  tools:
    - type: "mcp"
      value: "ember"
      description: "Ember MCP server"
      transport: "streamable_http"
      url: "https://ember-gateway-production.ember-shield-tp.workers.dev/mcp"
policy:
  allow_implicit_invocation: true
